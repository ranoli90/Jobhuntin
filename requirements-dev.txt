# =================================================================
# Development/Testing Dependencies
# Install: pip install -r requirements-dev.txt
# =================================================================

# ── Testing ──────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
psycopg2-binary>=2.9

# ── Linting ──────────────────────────────────────────────────────
ruff>=0.5.0
# black >=24.3.0 fixes PYSEC-2024-48
black>=24.3.0

# ── Type Checking ────────────────────────────────────────────────
mypy>=1.10

# ── Audit: Security, dead code, complexity (no sign-up required) ──
bandit>=1.7.0
pip-audit>=2.7.0
vulture>=2.11
radon>=6.0.0
semgrep>=1.95.0
detect-secrets>=1.5.0
deptry>=0.12.0
flake8>=7.0.0
