["Remove this useless assignment to variable \"setStepLoadingStates\".", "Refactor this function to reduce its Cognitive Complexity from 22 to the 15 allowed.", "Prefer `globalThis` over `window`.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.parseInt` over `parseInt`.", "Refactor this function to reduce its Cognitive Complexity from 22 to the 15 allowed.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `globalThis` over `window`.", "Prefer `globalThis` over `window`.", "Refactor this function to reduce its Cognitive Complexity from 21 to the 15 allowed.", "Prefer `globalThis` over `window`.", "Refactor this function to reduce its Cognitive Complexity from 22 to the 15 allowed.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.isNaN` over `isNaN`.", "Use <progress> instead of the \"progressbar\" role to ensure accessibility across all devices.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.parseInt` over `parseInt`.", "Prefer `Number.isNaN` over `isNaN`.", "Use <progress> instead of the \"progressbar\" role to ensure accessibility across all devices.", "Extract this nested ternary operation into an independent statement.", "Extract this nested ternary operation into an independent statement.", "Extract this nested ternary operation into an independent statement.", "Extract this nested ternary operation into an independent statement."]