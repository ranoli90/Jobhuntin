# Security constraints for pip install (optional)
# Use: pip install -r requirements.txt -c constraints.txt
# Note: markdownify constraint removed — python-jobspy 1.1.82 requires markdownify<0.14.0
# CVE-2025-46656 (LOW) fixed in 0.14.1; blocked until jobspy updates.
